#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>
void main(){
	char buf[1000];
	printf("Client 6 starts");
	fd = open("file2", read);
	for(int i = 0; i<5; i++){
		printf("Client 6 reads")
		read(fd, buf, 1000);
	}
	close(fd);
	printf("Client 6 ends");
}
